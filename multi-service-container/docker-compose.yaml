version: "3.8"

services:
  nginx:
    image: nginx:latest
    container_name: nginx_server
    ports:
      - "8080:80"
    restart: always

  apache:
    image: httpd:latest
    container_name: apache_server
    ports:
      - "8081:80"
    restart: always
